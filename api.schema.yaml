openapi: "3.0.3"

info:
  title: "Sample API"
  version: "1.0.0"

paths:
  "/api/v1/hello":
    get:
      summary: "Hello"
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema:
                type: string
                example: "hello"
